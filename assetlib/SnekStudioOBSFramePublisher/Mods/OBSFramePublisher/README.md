# OBSFramePublisher

OBSFramePublisher is a SnekStudio runtime mod that publishes the rendered viewport to the shared framebuffer used by the companion OBS source plugin.

Add this mod from `Mods -> Mod List` inside SnekStudio, then configure its output path and performance settings in the mod settings panel.
